using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class LookThenMove : MonoBehaviour
{
    
    public GameObject ground;

    // Update is called once per frame
    void Update()
    {

        //Local variables for camera, raycast, hitting, hitobject

        Transform camera = Camera.main.transform;

        Ray rayCast;

        RaycastHit hit;

        GameObject hitObj;

        //Create line along ray
        //Ray function
        //Ray variables need to 
        
    }
}
